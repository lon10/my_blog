- @posts.each do |post|
  .well
    %h3
      = link_to(post.title, home_post_path(post.slug))
    %p
      = text_preview(post.text).html_safe
    .small
      - if post.comments.count != 0
        = link_to("Комментариев: #{post.comments.count}", "#{home_post_path(post)}#comments")
      .pull-right
        = format_date(post.created_at)
= paginate @posts